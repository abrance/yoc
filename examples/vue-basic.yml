name: Vue CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  vue-ci:
    uses: owner/yoc/.github/workflows/vue-ci.yml@v1.0.0
    with:
      working-directory: .
      node-version: "20"
      package-manager: npm
      install-command: npm ci
      lint-command: npm run lint
      typecheck-command: npm run typecheck
      test-command: npm test
      build-command: npm run build
