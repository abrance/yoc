name: Helm CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  helm-ci:
    uses: owner/yoc/.github/workflows/helm-ci.yml@v1.0.0
    with:
      working-directory: charts
      helm-version: latest
      run-lint: true
      run-template: true
      run-unit: true