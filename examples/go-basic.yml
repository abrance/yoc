name: Go CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  go-ci:
    uses: owner/yoc/.github/workflows/go-ci.yml@v1
    with:
      working-directory: .
      go-version-file: go.mod
      cache-dependency-path: go.sum
      run-vet: true
      run-tests: true
      run-build: true
